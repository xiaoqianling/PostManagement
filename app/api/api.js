
/*
export const handlers = [rest.get('/posts', (request, response, context) => {
    return response(context.status(200), context.json({test:true}))
})]

export const worker = setupWorker(...handlers);*/
